# ✨ AI 上传指南：不会代码也能分享你的故事

你不需要懂任何代码，甚至不需要会写文章。只要会**复制、粘贴、点击**就够了。

整个流程只有 5 步，全程在浏览器里完成。

---

## 第 1 步：复制下面的提示词，发给任意 AI

把下面整段复制下来，粘贴给 Kimi、ChatGPT、DeepSeek、豆包等任何 AI，
然后把【】里的内容换成你自己的信息：

```
你是一名中文故事编辑。请帮我生成一篇关于我和小猫的 Markdown 故事文件。

我的信息：
- 我的昵称：【你的昵称】
- 小猫的名字：【小猫名字】
- 我们的故事主题：【比如：它怎么来到我家的 / 它陪我度过的低谷 / 它的趣事】
- 有没有封面照片：【有/没有】

请严格按以下要求输出：

1. 先输出一行文件路径，格式：
   src/content/stories/【昵称拼音】/【小猫名字拼音】.md
   （拼音全部小写，没有空格）

2. 然后输出完整的 Markdown 文件内容。文件开头必须是这段信息头
   （--- 之间的部分，字段名一个都不能改）：

---
title: 【故事标题】
author: 【你的昵称】
cover: /images/【昵称拼音】/【小猫名字拼音】.jpg
excerpt: 【50 字以内的一句话摘要，会显示在首页卡片上】
date: 【今天日期】
---

3. 正文 300~1000 字，语气温暖真实，像写给朋友的一封信。
   小标题随意。没有封面照片的话，删除 cover 那一行。

4. 如果正文需要插图，使用 Markdown 语法：
   ![图片说明](/images/【昵称拼音】/【照片文件名】.jpg)
   对应照片放在 public/images/【昵称拼音】/ 目录下。
   只使用 Markdown 插图语法，不要使用 HTML 的 <img> 标签。

5. 只输出文件路径和文件内容本身，不要输出任何解释。
```

> 💡 如果 AI 输出的内容不满意，直接对它说"再温柔一点"或"换一个标题"，改到满意为止。

## 第 2 步：在 GitHub 网页上创建文件

1. 打开本仓库（先点右上角 **Fork**，把它复制一份到你自己的账号下）
2. 在你的 Fork 里，依次点进 `src` → `content` → `stories`
3. 点 **Add file** → **Create new file**
4. 把 AI 给的"文件路径"粘贴到文件名框里（例如 `wang-xiaoyu/tuanzi.md`）
   —— 输入 `/` 会自动变成文件夹
5. 把 AI 给的文件内容粘贴到下面的大框里

## 第 3 步：上传封面照片（可选）

1. 点 **Add file** → **Upload files**
2. 进入 `public/images/` 下以你昵称拼音命名的文件夹（没有就新建）
3. 把照片拖进去，文件名改成和小猫名字拼音一致，如 `tuanzi.jpg`
4. 文件名必须和故事信息头里 `cover:` 写的一致

## 第 4 步：提交修改

页面拉到最下面，绿色按钮 **Commit changes** 直接点确认即可。

## 第 5 步：提交 Pull Request

1. 回到你 Fork 的仓库首页，会出现黄色提示条 → 点 **Contribute** → **Open pull request**
2. 标题写"添加【你的昵称】和【小猫名字】的故事"，点 **Create pull request**
3.  done！🎉 等待仓库主人审核合并后，你的故事就会出现在首页卡片墙上

---

## 📷 正文插图说明

正文里插照片很简单，用这一行 Markdown 即可：

```
![照片描述](/images/你的昵称拼音/照片名.jpg)
```

照片文件上传到 `public/images/你的昵称拼音/` 目录。
系统会自动处理网址前缀，你不需要写任何额外内容。
**注意：请使用 Markdown 的 ![]() 语法，不要用 HTML 的 <img> 标签**（后者图片会显示不出来）。

## ❓ 常见问题

**Q：没有小猫照片怎么办？**
A：删除信息头里的 `cover:` 那一行，系统会自动使用默认封面。

**Q：想修改已经提交的故事？**
A：在你的 Fork 里找到对应文件，点右上角铅笔编辑，重新提交 PR 即可。

**Q：完全卡住了？**
A：在仓库的 Issues 里留言，仓库主人会来帮你。
